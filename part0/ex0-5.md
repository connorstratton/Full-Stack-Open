(Assumes page has already been loaded)

```mermaid
sequenceDiagram
    participant browser
    participant server

  browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
  activate server
  Note over browser,server: user input is sent to the server
  server-->>browser: 201 created
  deactivate server

  Note right of browser: The json is directly udpated, and no further calls are made. correct list is reflected in JavaScript code
```
